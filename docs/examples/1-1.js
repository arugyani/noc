// const velXSlider = document.getElementById('velX')
// const velYSlider = document.getElementById('velY')

// const rectangle = {
//     x: 10,
//     y: 10,
//     velX: Number(velXSlider.value) ?? 1,
//     velY: Number(velYSlider.value) ?? 1 
// }
    // ctx.fillStyle = "#f4fff8"
    // ctx.fillRect(rectangle.x, rectangle.y, 10, 10);

    // velXSlider.addEventListener('input', (event) => {
//     let dir = rectangle.velX / Math.abs(rectangle.velX);

//     console.log(dir);
//     rectangle.velX = Number(event.target.value) * dir;
// })

// velYSlider.addEventListener('input', (event) => {
//     let dir = rectangle.velY / Math.abs(rectangle.velY);
//     if (dir == 0) dir = 1;

//     rectangle.velY = Number(event.target.value) * dir;
// })

    // const { x, y, velX, velY } = rectangle;

    // if (x + velX <= 0 || x + 10 + velX >= canvas.width) {
    //     rectangle.velX = -velX;
    // }

    // if (y + velY <= 0 || y + 10 + velY >= canvas.height) {
    //     rectangle.velY = -velY;
    // }

    // rectangle.x += rectangle.velX;
    // rectangle.y += rectangle.velY;

// export default function Example() {
    // const velX = document.getElementById()
// }

    // <div class="sliders">
    //     <label for="velX">velX</label>
    //     <input type="range" id="velX" min="1" max="10" step="0.01">
    //     <label for="velY">velY</label>
    //     <input type="range" id="velY" min="1" max="10" step="0.01">
    // </div>